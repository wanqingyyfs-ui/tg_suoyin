{
  "name": "tg-suoyin-root",
  "private": true,
  "scripts": {
    "install": "cd web && npm install",
    "build": "cd web && npm run build",
    "export": "cd web && npm run export",
    "rebuild": "python scripts/rebuild_index.py",
    "import-collected": "python scripts/import_collected_links.py",
    "dev": "cd web && npm run dev"
  }
}
